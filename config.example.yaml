frigate:
  url: http://YOUR_FRIGATE_IP:5000

mqtt:
  host: YOUR_MQTT_BROKER_IP
  port: 1883

telegram:
  token: "BOT_TOKEN"
  chat_id: "CHAT_ID"

notifications:
  detection:
    enabled: true
    labels: [person, car]
    min_score: 0.75
    send_snapshot: true
    cameras: []
